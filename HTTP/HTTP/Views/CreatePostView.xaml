<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HTTP.Views.CreatePostView"
             Title="CreatePost">
    <VerticalStackLayout>
        <Label 
            Text="{Binding Userid}"/>
        <Entry
            Text="{Binding Title}"/>
        <Label
            Text="Conteúdo da postagem"/>
        <Editor
            HeightRequest="150"
            Placeholder="Corpo do texto"
            Text="{Binding Body}"/>
    </VerticalStackLayout>
</ContentPage>